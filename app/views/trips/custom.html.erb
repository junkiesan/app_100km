<div class="map-container" id="custom-view">
  <!-- ICONS -->
  <div class="icon-sidebar">
    <a class="filter-btn" href="/" data-value=""><i class="fa fa-home"></i><p>Home</p></a>
    <a class="filter-btn" href="#" data-value=""><i class="fas fa-map"></i><p>Tous</p></a>
    <a class="filter-btn" href="#" data-value="Plage"><i class="fas fa-umbrella-beach"></i><p>Plages</p></a>
    <a class="filter-btn" href="#" data-value="Culture"><i class="fas fa-archway"></i><p>Culture</p></a>
    <a class="filter-btn" href="#" data-value="Foret"><i class="fas fa-hiking"></i><p>Randos</p></a>
    <a class="filter-btn" href="#" data-value="Parc" ><i class="fas fa-tree"></i><p>Parcs</p></a>
    <a class="filter-btn" href="#" data-value="Velo"><i class="fas fa-bicycle"></i><p>A vélo</p></a>
    <a class="filter-btn" href="#" data-value="Vues"><i class="fas fa-binoculars"></i><p>Vues</p></a>
<!--         <a class="filter-btn" href="#" data-value="autre"><i class="fas fa-ellipsis-h"></i><p>Autre</p></a>
 -->  </div>

  <div class="container d-none">
    <%= simple_form_for :filter, url: custom_trip_path, method: :get, remote: true do |f|%>
      <%= f.input :query %>
      <%= f.submit 'submit', id: 'submit-filter' %>
    <% end %>
  </div>


  <div id="map"
       style="width: 100%; height: 100vh;"
       data-venue-markers="<%= @venue_markers.to_json %>"
       data-trip-marker="<%= @trip_marker.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>

  <!-- VENUES -->
  <div class="venues-list">
    <%= render 'venues_list', venues: @venues %>
  </div>

<!-- BLUR -->
<!--   <div class="blur"></div>
</div> -->

