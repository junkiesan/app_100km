<%= simple_form_for [@venue, @review] do |f| %>
        <%= f.input :rating, collection: 0..5 %>
        <%= f.input :comment%>
        <%#R= f.input :photo as: :file%>
        <%= f.submit class: 'btn btn-primary' %>
      <% end %>
